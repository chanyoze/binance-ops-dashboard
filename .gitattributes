# 저장소에는 항상 LF 로 저장한다.
# 채점자가 macOS / Linux 에서 클론할 수 있으므로 줄바꿈을 정규화한다.
* text=auto eol=lf

# 셸 스크립트는 반드시 LF 여야 실행된다 (Docker 컨테이너 내부 실행 포함)
*.sh text eol=lf
*.mjs text eol=lf

# 바이너리
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
